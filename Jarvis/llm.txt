# 🤖 Jarvis: The MCP Automation Gateway

Jarvis is your autonomous "hands" for managing the Model Context Protocol (MCP) ecosystem.

## 📚 Documentation & Strategy
For the complete operational guide, standard, and architecture, read:
**`AGENTS.md`** (Located in the project root)

## 🛠️ Core Tool Reference

### 🚀 Initialization & Health
*   **`bootstrap_system()`**: Init package manager and infrastructure.
*   **`check_status()`**: Run system diagnostics (Doctor).

### 🧠 Profile & Client Management (The 3-Layer Stack)
*   **`manage_profile(action, name, ...)`**: Create/Edit server collections.
*   **`manage_client(action, client_name, ...)`**: Wire profiles to clients (Claude, Cursor, etc.).
*   **`suggest_profile(client_name)`**: Ask Jarvis which profiles should be active for the current directory.

### 📦 Server Management
*   **`install_server(name)`**: Install new capabilities.
*   **`search_servers(query)`**: Discover new tools.
*   **`list_servers()`**: inventory check.

### 🌐 Sharing
*   **`share_server(name)`**: Expose local tools via secure tunnel.

## 💡 Pro-Tip
Always prefer `manage_profile` and `manage_client` over manual configuration. Jarvis handles the wiring logic for you.
