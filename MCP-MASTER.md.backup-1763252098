# MCP-MASTER: Dynamic MCP Bus Architecture

**Version:** 2.0
**Date:** 2025-11-16
**Status:** ✅ **IMPLEMENTED** - stdio-based Cipher-aggregator

---

## 🎯 Architecture Decision Record

**Date**: 2025-11-16
**Decision**: Use Cipher-Aggregator with stdio protocol for IDE clients
**Status**: Implemented and Working

### Background

Initial research (see recovered-conversation-metamcp.md) proposed a **hybrid MetaMCP architecture**:
- MetaMCP as primary aggregator (ws://localhost:3000)
- Cipher in default mode as internal MCP server (http://localhost:3021)
- Rationale: Combine MetaMCP's Web UI + namespaces with Cipher's memory features

### Why We Chose Differently

| Factor | MetaMCP Hybrid Plan | **Actual: stdio Cipher** | Winner |
|--------|---------------------|--------------------------|---------|
| **Complexity** | High (2 systems, coordination) | Low (single system) | ✅ stdio |
| **IDE Compatibility** | Requires WebSocket support | Full stdio support | ✅ stdio |
| **Setup Time** | 4-5 weeks migration | Immediate | ✅ stdio |
| **Risk** | Breaking change, dual maintenance | Incremental improvements | ✅ stdio |
| **Memory Features** | Works but indirect path | Direct access | ✅ stdio |
| **Web UI** | ✅ Full featured | ❌ None | ⚠️ MetaMCP |
| **Namespaces** | ✅ Advanced | ❌ Manual config | ⚠️ MetaMCP |
| **Current State** | Would require full rebuild | ✅ Already working | ✅ stdio |

### Final Decision: stdio-based Cipher-Aggregator

**Rationale**:
1. **Working Now**: Cipher-aggregator + stdio already functional for both Cline and Kilo Code
2. **IDE Compatibility**: stdio is universally supported (vs MetaMCP's WebSocket requirement)
3. **Lower Risk**: Incremental improvements vs complete architecture change
4. **Simpler**: Single system to maintain, configure, and troubleshoot
5. **Future Path**: Can still migrate to MetaMCP later if needs justify complexity

**Trade-offs Accepted**:
- ❌ No Web UI for visual server management (acceptable - CLI works fine)
- ❌ No namespace auto-activation (acceptable - manual config is flexible)
- ❌ No advanced middleware (acceptable - basic routing works)

---

## Executive Summary

This document defines the **IMPLEMENTED stdio-based Cipher-Aggregator architecture** for managing MCP servers. The system provides a single point of integration for IDE clients (Cline, Kilo Code) with intelligent tool routing and Cipher's unique memory capabilities.

**Status Update**: We diverged from the original MetaMCP hybrid plan (see ADR above) in favor of a simpler, immediately functional stdio approach.

### Key Features
- ✅ **stdio Protocol**: Universal IDE compatibility (Cline, Kilo Code, any MCP client)
- ✅ **Working Now**: Fully operational, tested, documented
- ✅ **Dedicated Instances**: Each IDE gets isolated cipher process (no shared state issues)
- ✅ **Intelligent Memory**: Cipher stores tool knowledge and usage patterns
- ✅ **13 Internal Tools**: Memory operations, reasoning, bash execution
- ✅ **Multiple MCP Servers**: brave-search, context7, filesystem, httpie, pytest, etc.

### Critical Tool Name Reference

**⚠️ IMPORTANT: These are the CORRECT tool names** (Cline was using wrong names):

| ❌ WRONG NAME | ✅ CORRECT NAME | Server Source |
|---------------|-----------------|---------------|
| `write_to_file` | `write_file` | custom-filesystem-mcp |
| `read_from_file` | `read_text_file` | custom-filesystem-mcp |

See: [`/home/jrede/Cline/Rules/mcp-cipher-aggregator.md`](../Cline/Rules/mcp-cipher-aggregator.md) for full tool reference.

---

## Architecture Overview

### Current Implementation (stdio-based)

```
┌─────────────────────────────────────────────────────────────┐
│              IDE Clients (stdio per client)                 │
│  ┌──────────────┐              ┌──────────────┐            │
│  │    Cline     │              │   Kilocode   │            │
│  └──────┬───────┘              └──────┬───────┘            │
│         │                              │                    │
└─────────┼──────────────────────────────┼────────────────────┘
          │                              │
          ▼                              ▼
┌─────────────────────┐      ┌─────────────────────┐
│  Cipher Instance 1  │      │  Cipher Instance 2  │
│   (stdio for Cline) │      │ (stdio for Kilocode)│
│  Port: N/A (pipes)  │      │  Port: N/A (pipes)  │
└──────────┬──────────┘      └──────────┬──────────┘
           │                            │
           └──────────┬─────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│         Cipher-Aggregator Core (runs in each instance)      │
│  Environment:                                               │
│    MCP_SERVER_MODE=aggregator                               │
│    --mcp-transport-type=stdio                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │         Internal Cipher Tools (13 tools)              │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  • cipher_extract_and_operate_memory           │  │  │
│  │  │  • cipher_memory_search                        │  │  │
│  │  │  • cipher_store_reasoning_memory               │  │  │
│  │  │  • cipher_extract_reasoning_steps              │  │  │
│  │  │  • cipher_evaluate_reasoning                   │  │  │
│  │  │  • cipher_search_reasoning_patterns            │  │  │
│  │  │  • cipher_bash                                 │  │  │
│  │  │  • brave_web_search                            │  │  │
│  │  │  • brave_local_search                          │  │  │
│  │  │  • brave_video_search                          │  │  │
│  │  │  • brave_image_search                          │  │  │
│  │  │  • brave_news_search                           │  │  │
│  │  │  • brave_summarizer                            │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                              │
│              Aggregated MCP Servers (from cipher.yml)       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  ├─ routing-metadata  (stdio) - Tool validation   │  │
│  │  ├─ httpie            (stdio) - HTTP/API testing  │  │
│  │  ├─ schemathesis      (stdio) - API schema testing│  │
│  │  ├─ filesystem        (stdio) - File operations   │  │
│  │  ├─ file-batch        (stdio) - Batch file ops    │  │
│  │  ├─ pytest            (stdio) - Python testing    │  │
│  │  ├─ context7          (SSE) - llm.txt docs        │  │
│  │  ├─ morph-fast-apply  (SSE) - Code editing        │  │
│  │  ├─ gpt-researcher    (stdio) - Research           │  │
│  │  └─ memory-bank       (stdio) - Persistent memory  │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**Key Point**: We run BOTH modes simultaneously:
- stdio instances for IDE clients (isolated, dedicated)
- streamable-HTTP instance for web/testing (shared, managed by mcp-manager.sh)

---

### Alternative: streamable-HTTP (for web clients)

```
┌─────────────────────────────────────────────────────────────┐
│         Cipher-Aggregator (streamable-HTTP mode)            │
│              http://127.0.0.1:3020/http                     │
│                                                              │
│  Started via: ./mcp-manager.sh start                        │
│  Use case: Web clients, testing, external integrations      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  mcp-manager.sh (Process Manager)                    │  │
│  │  ├─ Health checks every 30s                          │  │
│  │  ├─ Auto-restart on crash                            │  │
│  │  ├─ Log rotation and cleanup                         │  │
│  │  └─ PID file: cipher-aggregator.pid                  │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**Key Point**: We run BOTH modes simultaneously:
- stdio instances for IDE clients (isolated, dedicated)
- streamable-HTTP instance for web/testing (shared, managed by mcp-manager.sh)

---

## ✅ Implemented Features (2025-11-16)

### stdio-based IDE Integration
- ✅ Cline configured with stdio transport
- ✅ Kilo Code configured with stdio transport
- ✅ Environment variables explicitly passed to subprocesses
- ✅ Both IDEs tested and working
- ✅ Global rules created: `/home/jrede/Cline/Rules/mcp-cipher-aggregator.md`

### Critical Bugs Fixed
- ✅ **routing-metadata-mcp.py**: Fixed init_logging() TypeError (was blocking server startup)
- ✅ **All Ruff linting errors**: Fixed unused imports, variables
- ✅ **All Pyrefly type errors**: Fixed missing imports, type annotations
- ✅ **Tool name corrections**: Documented write_file vs write_to_file issue

### Documentation Created
- ✅ [`docs/GLOBAL-MCP-CONFIG.md`](docs/GLOBAL-MCP-CONFIG.md) - Configuration guide
- ✅ [`docs/resolved-tickets/2025-11-15-cline-mcp-connection-stdio-fix.md`](docs/resolved-tickets/2025-11-15-cline-mcp-connection-stdio-fix.md) - Detailed troubleshooting
- ✅ [`/home/jrede/Cline/Rules/mcp-cipher-aggregator.md`](../Cline/Rules/mcp-cipher-aggregator.md) - Cline-specific rules

---

## ⏸️ Deferred: MetaMCP Hybrid Migration

The recovered conversation (see `recovered-conversation-metamcp.md`) shows a comprehensive plan for a **MetaMCP hybrid architecture**. This is now **DEFERRED** based on the decision above.

**If reconsidering later**, the plan includes:
- 5-phase migration (4-5 weeks)
- MetaMCP as primary aggregator with Web UI
- Cipher in default mode as memory-specialized server
- Advanced namespace and middleware configuration

**Why deferred**:
- Current stdio setup is working perfectly
- Migration would be disruptive
- Benefits don't justify complexity at this time
- Can revisit if needs change (Web UI becomes critical, namespace complexity increases)

---

## Current Configuration (Working Setup)

### Cline MCP Settings

**File**: `~/.vscode-server/data/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`

```json
{
  "mcpServers": {
    "cipher-aggregator": {
      "command": "node",
      "args": [
        "/home/jrede/.npm-global/bin/cipher",
        "--mode", "mcp",
        "--agent", "/home/jrede/dev/MCP/cipher.yml",
        "--mcp-transport-type", "stdio"
      ],
      "env": {
        "MCP_SERVER_MODE": "aggregator",
        "OPENROUTER_API_KEY": "...",
        "OPENAI_API_KEY": "...",
        "BRAVE_API_KEY": "...",
        "TAVILY_API_KEY": "..."
      },
      "disabled": false,
      "autoApprove": ["WriteTo", "write_file", "CopyTo", "copy_to_clipboard", "PasteFrom", "paste_from_clipboard
      // add more tools as necessary
      */
      return {
        tool: argOptions.tool || tool,
        arguments,
      };
    }

    // ... resetResults ...

    // ... other functions ...

    // ... existing code ...
  }
}
```

### Kilo Code MCP Settings

**File**: `~/.vscode-server/data/User/globalStorage/kilocode.kilo-code/settings/mcp_settings.json`

```json
{
  "mcpServers": {
    "cipher-aggregator": {
      "command": "node",
      "args": [
        "/home/jrede/.npm-global/bin/cipher",
        "--mode", "mcp",
        "--agent", "/home/jrede/dev/MCP/cipher.yml",
        "--mcp-transport-type", "stdio"
      ],
      "env": {
        "MCP_SERVER_MODE": "aggregator",
        "OPENROUTER_API_KEY": "...",
        "OPENAI_API_KEY": "...",
        "BRAVE_API_KEY": "...",
        "TAVILY_API_KEY": "..."
      },
      "disabled": false,
      "alwaysAllow": ["transport/V1/echo/echo"]
    }
  }
}
```

### Why This Works

1. **stdio Protocol**: IDE spawns child process, communicates via stdin/stdout pipes
2. **Explicit Environment**: All API keys passed directly (no reliance on .env files)
3. **Dedicated Instances**: Each IDE gets isolated cipher process (no conflicts)
4. **No Session Management**: Direct pipe communication (no HTTP handshake needed)

See: [`docs/GLOBAL-MCP-CONFIG.md`](docs/GLOBAL-MCP-CONFIG.md) for detailed explanation.

---

## Original Plan (Now Reference Only)

The sections below represent the original architectural plan for Cipher-aggregator as a single shared HTTP endpoint. This has been superseded by the stdio approach above, but is retained for reference.

---

## Phase 1: Research & Discovery (COMPLETED)

### 1.1 Fix Cipher Aggregator MCP Server Loading (✅ DONE)

**Original Issue:** Cipher logs showed "Using simplified configuration with env vars" instead of loading mcpServers from cipher.yml.

**Resolution:** This was NOT a bug - Cipher was working correctly. The stdio model doesn't use the shared HTTP instance (which DOES load mcpServers), it uses dedicated per-client instances.

**Research Complete:**

- [x] Determined stdio vs streamable-HTTP protocol differences
- [x] Verified mcpServers DO load in streamable-HTTP mode (mcp-manager.sh)
- [x] Confirmed environment variables work correctly
- [x] No version upgrade needed

**Success Achieved:**

- ✅ Cipher loads mcpServers in streamable-HTTP mode
- ✅ Log shows "MCP Manager: Initializing 0 servers" for stdio (expected - internal tools only)
- ✅ All 13 internal Cipher tools available in both modes

### 1.2 Research Alternative Aggregators (✅ DONE)

**Research Completed:**

- [x] MCPJungle: 692 stars, basic tool groups, no Web UI
- [x] MetaMCP: 1.7k stars, full Web UI, advanced namespaces
- [x] Cipher: 600 stars, unique memory features, aggregator mode

**Decision Made:** Stick with Cipher-aggregator + stdio (see ADR above)

---

## Appendix A: Tool Name Reference (Critical)

### File Operations

**custom-filesystem-mcp.py provides:**

- ✅ `write_file` (path, content) - Write/overwrite file
- ✅ `read_text_file` (path, head?, tail?) - Read file content
- ✅ `read_multiple_files` (paths[]) - Read multiple files
- ✅ `list_directory` (path) - List directory contents
- ✅ `create_directory` (path) - Create directory

**⚠️ Common Mistakes:**

- ❌ `write_to_file` - DOES NOT EXIST
- ❌ `read_from_file` - DOES NOT EXIST
- ❌ `create_file` - DOES NOT EXIST (use `write_file`)

### Search & Research

**Brave Search Tools:**

- `brave_web_search` - General web search
- `brave_local_search` - Local businesses/places
- `brave_news_search` - News articles
- `brave_video_search` - Video content
- `brave_image_search` - Images
- `brave_summarizer` - AI summaries of search results

**Other Search:**

- `cipher_workspace_search` - Search project codebase
- `gpt_researcher` - Comprehensive AI research

### Memory & Knowledge

- `cipher_extract_and_operate_memory` - Extract and store knowledge
- `cipher_memory_search` - Semantic search stored knowledge
- `cipher_store_reasoning_memory` - Store reasoning traces
- `cipher_extract_reasoning_steps` - Extract reasoning from text
- `cipher_evaluate_reasoning` - Evaluate reasoning quality
- `cipher_search_reasoning_patterns` - Find similar reasoning
- `memory_bank_write` - Persistent memory storage
- `memory_bank_read` - Read persistent memory

---

## Appendix B: Troubleshooting

### Error: "Missing value for required parameter 'content'"

**Cause**: Using wrong tool name `write_to_file` instead of `write_file`
**Solution**: Use correct tool name `write_file`
```json
// ❌ WRONG
{"tool": "write_to_file", "arguments": {"path": "file.txt"}}

// ✅ CORRECT
{"tool": "write_file", "arguments": {"path": "file.txt", "content": "..."}}
```

### Error: "SSE error: Non-200 status code (400)"

**Cause**: Using streamable-HTTP URL instead of stdio
**Solution**: Use stdio configuration (see current config above)

### Error: "No API key found"

**Cause**: Missing environment variables in subprocess
**Solution**: Add API keys to `env` object in MCP settings

### Error: "tool_use without tool_result"

**Cause**: Corrupted conversation state in current task
**Solution**: Close task and start new one

---

## Appendix C: Related Documentation

- [`docs/GLOBAL-MCP-CONFIG.md`](docs/GLOBAL-MCP-CONFIG.md) - Complete configuration guide
- [`docs/resolved-tickets/2025-11-15-cline-mcp-connection-stdio-fix.md`](docs/resolved-tickets/2025-11-15-cline-mcp-connection-stdio-fix.md) - stdio migration details
- [`/home/jrede/Cline/Rules/mcp-cipher-aggregator.md`](../Cline/Rules/mcp-cipher-aggregator.md) - Cline-specific rules
- [`recovered-conversation-metamcp.md`](recovered-conversation-metamcp.md) - Original MetaMCP hybrid plan
- [`cipher.yml`](cipher.yml) - Cipher configuration
- [`.env`](.env) - Environment variables (reference - not auto-loaded by stdio)

---
